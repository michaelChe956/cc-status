# cc-statusline 新功能使用指南

## 快速开始

### 1. 安装项目
```bash
# 方式 1: 使用 uvx (推荐)
uvx cc-statusline install --interactive

# 方式 2: 本地开发
git clone https://github.com/michaelche/cc-statusline
cd cc-statusline
uv venv
source .venv/bin/activate
uv pip install -e ".[dev]"
```

---

## 功能一：配置验证

### 基础验证
```bash
# 快速验证
cc-statusline verify

# 输出:
# ✅ 状态栏配置验证通过
# {
#   "type": "command",
#   "command": "uvx cc-statusline --once --theme modern",
#   ...
# }
```

### 详细验证
```bash
# 显示详细信息
cc-statusline verify --verbose

# 输出:
# 🔍 开始验证 cc-statusline 配置...
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━��━━━━━━━━━━━━━━━
# ✅ 配置文件存在: ~/.claude/settings.json
# ✅ 配置格式有效: JSON 解析成功
# ✅ 状态栏配置存在
# ✅ 必需字段完整: type, command
# ✅ type 字段正确: command
# ✅ 命令路径有效: uvx cc-statusline --once --theme modern
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ✅ 状态栏配置验证通过
```

### 测试命令执行
```bash
# 验证并测试命令
cc-statusline verify --test
```

### 完整健康检查
```bash
# 生成健康报告
cc-statusline verify --health

# 输出:
# 🔍 cc-statusline 健康检查报告
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ✅ 配置文件: ~/.claude/settings.json
# ✅ 配置格式: JSON 有效
# ✅ 状态栏配置: 已配置
# ✅ 命令路径: uvx cc-statusline --once --theme modern
# ✅ 命令执行: 测试通过
# ℹ️  版本: 0.2.0
# ━━━━━━━━━━━━━━━━━━━���━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 状态: 健康 ✅
```

---

## 功能二：配置迁移

### 导出配置
```bash
# 导出到文件（包含元数据）
cc-statusline export ./my-statusline-config.yaml

# 导出不含元数据
cc-statusline export ./my-config.yaml --no-metadata

# 输出:
# ✅ 配置已导出: ./my-statusline-config.yaml
```

### 配置文件格式
```yaml
# my-statusline-config.yaml
version: "0.2.0"
exported_at: "2026-01-28T17:30:00"
statusLine:
  type: command
  command: "uvx cc-statusline --once --theme modern"
  refreshInterval: 10000
  padding: 0
```

### 导入配置
```bash
# 导入配置
cc-statusline import ./my-statusline-config.yaml

# 如果已存在配置，会提示:
# ⚠️  状态栏配置已存在
# 当前配置: {...}
# 使用 --force 强制覆盖

# 强制覆盖导入
cc-statusline import ./my-config.yaml --force

# 输出:
# ✅ 已备份原配置: ~/.claude/settings.json.backup.20260128_173000
# ✅ 配置已导入
# {
#   "type": "command",
#   ...
# }
#
# 重启 Claude Code 以应用更改
```

### 使用场景

**场景 1: 多机同步**
```bash
# 机器 A
cc-statusline export ~/Dropbox/cc-statusline.yaml

# 机器 B
cc-statusline import ~/Dropbox/cc-statusline.yaml --force
```

**场景 2: 配置备份**
```bash
# 备份当前配置
cc-statusline export ./backup/statusline-$(date +%Y%m%d).yaml

# 恢复配置
cc-statusline import ./backup/statusline-20260128.yaml --force
```

**场景 3: 团队分享**
```bash
# 导出团队标准配置
cc-statusline export ./team-statusline.yaml --no-metadata

# 团队成员导入
cc-statusline import ./team-statusline.yaml --force
```

---

## 功能三：交互式安装

### 启动交互式安装
```bash
cc-statusline install --interactive
```

### 交互流程

#### 步骤 1: 选择主题
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎨 cc-statusline 交互式安装向导
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 步骤 1/3: 选择主题
────────────────────────────────────────────────────────

可用主题:
  1. default
  2. modern
  3. minimal
  4. nerd
  5. powerline

请输入主题名称或序号 (默认: modern, q 取消):
```

**提示**:
- 输入数字 1-5 快速选择
- 输入主题名称精确选择
- 按 Tab 键自动补全主题名称
- 输入 `q` 取消安装
- 直接按回车使用默认值

#### 步骤 2: 配置刷新间隔
```
📋 步骤 2/3: 配置刷新间隔
────────────────────────────────────────────────────────

推荐值:
  5000ms  - 快速刷新（5秒）
  10000ms - 标准刷新（10秒，推荐）
  30000ms - 慢速刷新（30秒）

请输入刷新间隔(ms) (1000-60000, 默认: 10000, q 取消):
```

**提示**:
- 范围: 1000-60000 毫秒
- 推荐: 10000ms (10秒)
- 较低值: 更及时但消耗资源
- 较高值: 节省资源但更新慢

#### 步骤 3: 确认安装
```
📋 步骤 3/3: 确认配置
────────────────────────────────────────────────────────

配置摘要:
  主题: modern
  刷新间隔: 10000ms

确认安装? (Y/n): Y
```

#### 安装完成
```
🚀 开始安装...
✅ 已备份原配置: ~/.claude/settings.json.backup.20260128_173500
✅ 状态栏配置已安装
命令: uvx cc-statusline
主题: modern
刷新间隔: 10000ms

重启 Claude Code 以应用更改

✅ 安装完成！

下一步:
  1. 重启 Claude Code 以应用更改
  2. 使用 'cc-statusline verify --health' 验证配置
```

### 快捷操作

| 按键 | 功能 |
|------|------|
| `Tab` | 主题名称自动补全 |
| `Enter` | 确认/使用默认值 |
| `q` | 取消操作 |
| `Ctrl+C` | 终止安装 |

---

## 功能四：发布到 PyPI

### 本地构建测试
```bash
# 运行发布脚本
./scripts/publish.sh

# 手动步骤
python -m build
twine check dist/*
```

### 发布到 TestPyPI
```bash
# 配置 TestPyPI token
export TWINE_USERNAME=__token__
export TWINE_PASSWORD=<your-testpypi-token>

# 上传
twine upload --repository testpypi dist/*

# 测试安装
uvx --index-url https://test.pypi.org/simple/ cc-statusline --version
uvx --index-url https://test.pypi.org/simple/ cc-statusline install --interactive
```

### 正式发布到 PyPI
```bash
# 配置 PyPI token
export TWINE_USERNAME=__token__
export TWINE_PASSWORD=<your-pypi-token>

# 上传
twine upload dist/*

# 验证安装
uvx cc-statusline --version
uvx cc-statusline install --interactive
```

### GitHub Actions 自动发布

#### 手动触发
1. 进入 GitHub Actions 页面
2. 选择 "Publish to PyPI" workflow
3. 点击 "Run workflow"
4. 选择环境 (testpypi/pypi)
5. 点击 "Run workflow" 确认

#### 自动触发
- 创建 GitHub Release 时自动发布到 PyPI

---

## 完整工作流示例

### 新机器安装配置
```bash
# 1. 使用 uvx 直接安装
uvx cc-statusline install --interactive

# 2. 按照交互提示完成配置
# 3. 重启 Claude Code

# 4. 验证配置
uvx cc-statusline verify --health
```

### 配置迁移工作流
```bash
# 机器 A: 导出配置
cc-statusline export ~/my-config.yaml

# 传输文件到机器 B
scp ~/my-config.yaml user@machine-b:~/

# 机器 B: 导入配置
cc-statusline import ~/my-config.yaml --force
```

### 开发测试工作流
```bash
# 1. 克隆仓库
git clone https://github.com/michaelche/cc-statusline
cd cc-statusline

# 2. 设置开发环境
uv venv
source .venv/bin/activate
uv pip install -e ".[dev]"

# 3. 运行质量检查
black src/ tests/
ruff check src/ tests/
mypy src/

# 4. 运行测试
pytest tests/ -v

# 5. 测试交互式安装
python -m cc_statusline install --interactive

# 6. 验证配置
python -m cc_statusline verify --health
```

---

## 故障排查

### 问题 1: 导入失败
```bash
# 错误: ❌ 配置文件格式错误

# 解决:
# 1. 检查 YAML 格式
cat my-config.yaml

# 2. 验证配置完整性
grep -E "(statusLine|type|command)" my-config.yaml

# 3. 重新导出
cc-statusline export ./new-config.yaml
```

### 问题 2: 验证失败
```bash
# 错误: ❌ 命令执行测试失败

# 解决:
# 1. 检查命令是否可用
uvx cc-statusline --version

# 2. 检查配置文件
cat ~/.claude/settings.json | jq '.statusLine'

# 3. 手动测试命令
uvx cc-statusline --once
```

### 问题 3: 交互式安装中断
```bash
# 如果中途中断，可以:

# 1. 直接重新运行
cc-statusline install --interactive

# 2. 或使用非交互式安装
cc-statusline install --theme modern --interval 10000 --force
```

---

## 更多资源

- [GitHub Repository](https://github.com/michaelche/cc-statusline)
- [PyPI Package](https://pypi.org/project/cc-statusline/)
- [实施报告](./2026-01-28_实施报告_四项功能增强_v1.0.md)
